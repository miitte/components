class {
  handleClick(emit, e) {
    e.preventDefault()
    emit('route', new URL(this.input.href, document.baseURI).pathname)
  }
}
context|{ route }, emit| from='miitte-router'
  ---
  <a href=input.href on-click('handleClick', emit)>
    <${input.renderBody}/>
  </a>
  ---
